NodesList:
  allOf:
    - $ref: "#/components/schemas/Iterable" 
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Node" 
Node:
  type: object
  properties:
    mac:
      type: string
      description: The MAC address of the device
    

