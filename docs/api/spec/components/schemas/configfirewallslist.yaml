ConfigFirewallsList:
  allOf:
  - $ref: '#/static/components/schemas/Iterable'
  - properties:
      items:
        description: List
        items:
          $ref: '#/components/schemas/ConfigFirewall'
        type: array
    type: object
  description: List
