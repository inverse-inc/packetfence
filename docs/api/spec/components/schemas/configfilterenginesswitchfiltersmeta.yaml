ConfigFilterEnginesSwitchFiltersMeta:
  properties:
    meta:
      properties:
        condition:
          $ref: '#/components/schemas/Meta'
        description:
          $ref: '#/components/schemas/Meta'
        id:
          $ref: '#/components/schemas/Meta'
        log:
          $ref: '#/components/schemas/Meta'
        params:
          $ref: '#/components/schemas/Meta'
        scopes:
          $ref: '#/components/schemas/Meta'
        status:
          $ref: '#/components/schemas/Meta'
        switch:
          $ref: '#/components/schemas/Meta'
      type: object
  type: object
