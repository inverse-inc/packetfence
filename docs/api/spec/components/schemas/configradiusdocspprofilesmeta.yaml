ConfigRadiusdOCSPProfilesMeta:
  properties:
    meta:
      properties:
        id:
          $ref: '#/components/schemas/Meta'
        ocsp_enable:
          $ref: '#/components/schemas/Meta'
        ocsp_override_cert_url:
          $ref: '#/components/schemas/Meta'
        ocsp_softfail:
          $ref: '#/components/schemas/Meta'
        ocsp_timeout:
          $ref: '#/components/schemas/Meta'
        ocsp_url:
          $ref: '#/components/schemas/Meta'
        ocsp_use_nonce:
          $ref: '#/components/schemas/Meta'
      type: object
  type: object
