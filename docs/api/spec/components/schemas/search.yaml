Search:
  type: object
  properties:
    fields:
        type: string
    limit:
        type: string
    sort:
        type: string
    cursor:
        type: string
    query:
        $ref: '#/components/schemas/Query'
