/scan:
  post:
    tags:
      - SnmpScan
    summary: Scan a network with given addresses and credentials.
    description: The task can take some time.
    operationId: snmpScan
    requestBody:
      $ref: "#/components/requestBodies/SnmpScanPayload"
    responses:
      "200":
        $ref: "#/components/responses/Async"
      "400":
        $ref: "#/components/responses/BadRequest"
