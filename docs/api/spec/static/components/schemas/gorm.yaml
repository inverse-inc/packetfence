Gorm:
  type: object
  properties:
    items:
      type: array
      items:
        type: object
        properties:
          ID:
            description: '`PRIMARY KEY`'
            type: integer
          CreatedAt:
            type: string
            format: date-time
          DeletedAt:
            type: string
            format: date-time
          UpdatedAt:
            type: string
            format: date-time
          Ctx:
            type: string
          DB:
            type: object
            properties:
              Error:
                type: string
              RowsAffected:
                type: integer
              Value:
                type: string
