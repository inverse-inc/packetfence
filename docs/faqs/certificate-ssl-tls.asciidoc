== Certificate & SSL/TLS Management


[[radius-certificate-renewal]]
=== RADIUS SSL certificate renewal

*Problem:* RADIUS SSL certificate is expiring and needs renewal without
service interruption^[1]^

*Symptoms:*

* Certificate expiration warnings in RADIUS logs
* EAP-TLS authentication failures
* Certificate validation errors in client logs
* "certificate verify failed" errors
* Authentication works for some protocols but not certificate-based ones

*Solution:*

*Step-by-step renewal process:*

. *Navigate to PacketFence directory:*
+
----
cd /usr/local/pf
----

. *Backup existing certificate:*
+
----
cp conf/ssl/server.crt conf/ssl/server.crt.bak
cp conf/ssl/server.key conf/ssl/server.key.bak
----

. *Generate new certificate:*
+
----
make conf/ssl/server.crt
----

. *Restart RADIUS service:*
+
----
/usr/local/pf/bin/pfcmd service radiusd restart
----

. *Verify certificate:*
+
----
openssl x509 -in conf/ssl/server.crt -text -noout | grep "Not After"
----

*Affected Versions:* All PacketFence versions

*Community Reference:*
1. https://sourceforge.net/p/packetfence/mailman/message/58734583/[Questions regarding the setup of LetsEncrypt]

'''


