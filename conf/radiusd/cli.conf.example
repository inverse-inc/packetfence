pidfile = %%pid_file%%

$INCLUDE radiusd.conf

listen {
    ipaddr = %%management_ip%%
    port = 1814
    type = auth
    virtual_server = packetfence-cli
}

listen {
    type = control
    socket = %%socket_file%%
    mode = rw
}
