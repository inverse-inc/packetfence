[defaults]
# change display of log
stdout_callback = yaml

# disable host key checking
host_key_checking = False

# forks
forks = 50

# Collections and roles resolve from ansible's default user path
# (/root/.ansible in the vagrant-build image, where the Dockerfile bakes them
# from requirements.yml so builds skip the galaxy download).
