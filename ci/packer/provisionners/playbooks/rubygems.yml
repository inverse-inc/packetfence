- hosts: rubygems
  name: install ruby gems
  become: True

  tasks:
    - name: Install asciidoctor gems
      gem:
        name: "{{ item['name'] }}"
        state: latest
        user_install: no
      loop: '{{ asciidoctor_gems }}'
