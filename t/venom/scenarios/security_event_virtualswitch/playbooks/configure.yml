---
# Pre-configuration for security_event_virtualswitch scenario
# This playbook runs before the venom tests

- hosts: pfservers
  name: Configure security event virtualswitch environment
  become: True

  tasks:
    - name: Ensure virtualswitch package is available
      debug:
        msg: "VirtualSwitch will be installed by the test suite"
