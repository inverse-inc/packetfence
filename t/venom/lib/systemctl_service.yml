executor: systemctl_service
input:
  unit_command: ""
  service: ""
  sudo: ""
steps:
- type: exec
  script: '{{.input.sudo}} systemctl {{.input.unit_command}} {{.input.service}}'
  vars:
    status_lines:
      from: result.systemout

output:
  service_status: "{{.status_lines}}"
