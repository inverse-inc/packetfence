executor: check_host_internet_access
input:
  host: "{{.node01_mgmt_ip}}"
  user: "{{.ssh_user}}"
steps:
  - type: ssh
    host: '{{.input.host}}'
    user: '{{.input.user}}'
    command: |
      cd /usr/local/pf/t/venom ; \
      sudo /usr/local/pf/t/venom/venom-wrapper.sh test_suite//common/check_internet_access.yml
