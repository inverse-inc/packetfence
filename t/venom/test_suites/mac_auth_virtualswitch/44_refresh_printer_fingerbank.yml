name: Trigger Fingerbank refresh for printer device
testcases:

- name: refresh_printer_fingerbank
  retry: 6
  delay: 10
  steps:
  - type: pf_api_node_action
    method: POST
    id: '{{.virtualswitch.printer.mac_address}}'
    url_extra: '/fingerbank_refresh'
    status_code: 200
