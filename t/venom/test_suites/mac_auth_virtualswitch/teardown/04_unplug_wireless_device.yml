name: Unplug device via virtualswitch API (interface 7)
testcases:

- name: unplug_device
  steps:
  - type: virtualswitch_unplug_device
    api_url: '{{.virtualswitch.api.url}}'
    interface_id: '{{.wireless_mac_auth_virtualswitch.device.interface.id}}'
