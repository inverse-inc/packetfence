name: Teardown
testcases:
- name: get_login_token
  steps:
  - type: get_login_token

- name: delete_all_backups
  steps:
  - type: pf_files_backup_delete

- name: delete_user_in_db
  steps:
  - type: pf_api_user_delete
    user: "{{.backup_db_and_restore.user}}"
