name: Check RADIUS audit log
testcases:
- name: pf_api_check_radius_audit_log
  steps:
  - type: pf_api_check_radius_audit_log
    mac_address: "{{.node01_ens7_mac_address}}"
    connection_type_value: "{{.captive_portal.profiles.filters.connection_type}}"
    vlan_registration: "{{.roles.registration.vlan_id}}"
    profile:  "{{.captive_portal.profiles.id}}"
