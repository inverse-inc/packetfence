name: Delete iptable rule
testcases:
# temp, as a workaround
- name: delete_fd_focker_dnat_rules
  steps:
  - type: exec
    script: perl -I/usr/local/pf/lib -I/usr/local/pf/lib_perl/lib/perl5/ '-Mpf::config' '-Mpf::firewalld' -e 'pf::firewalld::fd_docker_dnat_rules("remove")'
