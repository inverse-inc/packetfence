name: Delete headless_device role
testcases:
- name: get_login_token
  steps:
  - type: get_login_token

- name: delete_headless_device_role
  steps:
  - type: http
    method: DELETE
    url: '{{.pfserver_webadmin_url}}/api/v1/config/role/{{.wired_mac_auth_virtualswitch.roles.headless_device.id}}'
    ignore_verify_ssl: true
    headers:
      "Authorization": "{{.get_login_token.result.token}}"
      "Content-Type": "application/json"
    assertions:
      - result.statuscode ShouldEqual 200
