name: Unplug 802.1X device via virtualswitch API
testcases:

- name: unplug_dot1x_device
  steps:
  - type: virtualswitch_unplug_device
    api_url: '{{.virtualswitch.api.url}}'
    interface_id: '{{.virtualswitch.dot1x.computer.interface.id}}'
