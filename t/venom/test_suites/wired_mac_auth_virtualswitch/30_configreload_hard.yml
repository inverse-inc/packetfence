name: Reload pf config so the vlan_filters validator sees the printer role created in global_config_virtualswitch
testcases:

- name: run_configreload_hard
  steps:
  - type: pf_configreload_hard
    time_to_sleep: 5
