name: Unplug device via virtualswitch API (interface 1)
testcases:

- name: unplug_device
  steps:
  - type: virtualswitch_unplug_device
    api_url: '{{.virtualswitch.api.url}}'
    interface_id: '{{.virtualswitch.interface.id}}'
