name: Plug device via virtualswitch REST API with MAB
testcases:

- name: plug_device_with_mab
  steps:
  - type: virtualswitch_plug_device
    api_url: '{{.virtualswitch.api.url}}'
    interface_id: '{{.virtualswitch.interface.id}}'
    mac_address: '{{.virtualswitch.device.mac_address}}'
    auth_method: mab
    profile: '{{.virtualswitch.profile.name}}'

- name: wait_for_radius_auth
  steps:
  - type: sleep
    time_to_sleep: 10
