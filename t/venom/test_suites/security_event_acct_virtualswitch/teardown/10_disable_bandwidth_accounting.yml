name: Disable bandwidth accounting in RADIUS configuration
testcases:

- name: disable_bandwidth_accounting
  steps:
  - type: pf_api_config_action
    id: "base/radius_configuration"
    method: PATCH
    body: >-
      {
        "process_bandwidth_accounting": "false"
      }
    status_code: 200
