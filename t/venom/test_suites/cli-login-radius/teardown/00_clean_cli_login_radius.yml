name: Teardown
testcases:
- name: get_login_token
  steps:
  - type: get_login_token

- name: delete radius source
  steps:
  - type: delete_source
    id: "{{.cli_login_radius.sources.id}}"
    token: "{{.get_login_token.json.result.token}}"
