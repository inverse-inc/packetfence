iname: Deploy certificates on PF
testcases:
  - name: copy_certificates
    steps:
      - type: exec
        script:  |
          sudo cp -v {{.wired_dot1x_eap_tls_pki.paths.per_client_directory}}/* /etc/wpa_supplicant/eap_tls_pki/
