name: Check logs on eapol-test results
testcases:
- name: check_eapol_test_logs
  steps:
  - type: exec
    script: |
      LOG_FILE="/usr/local/pf/logs/eapol_test.log"; tail -n 1 "$LOG_FILE"
    timeout: 10
    assertions:
      - result.systemout ShouldContainSubstring "SUCCESS"
