name: Delete Inheritance2 role
testcases:
- name: get_login_token
  steps:
  - type: get_login_token

- name: delete_inheritance2_role
  steps:
  - type: http
    method: DELETE
    url: '{{.pfserver_webadmin_url}}/api/v1/config/role/{{.vlan_inheritance_virtualswitch.roles.inheritance2.id}}'
    ignore_verify_ssl: true
    headers:
      "Authorization": "{{.get_login_token.result.token}}"
      "Content-Type": "application/json"
    assertions:
      - result.statuscode ShouldEqual 200
