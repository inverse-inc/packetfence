name: Seed Fingerbank API key from psonoci
testcases:
- name: seed_fingerbank_config
  steps:
  - type: exec
    script: |
      cp {{.venom_dir}}/test_suites/configurator_bypass/seeds/.rendered/fingerbank-upstream.conf /usr/local/fingerbank/conf/fingerbank.conf
    assertions:
      - result.code ShouldEqual 0
