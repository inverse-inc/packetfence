name: Release DHCP on node01
testcases:
  - name: release_dhcp
    steps:
      - type: ssh
        host: '{{.node01_mgmt_ip}}'
        user: '{{.ssh_user}}'
        command:  |
          cd /usr/local/pf/t/venom ; \
          sudo /usr/local/pf/t/venom/venom-wrapper.sh {{.nodes_test_suite_dir}}/dot1x_eap_peap/{{.venom.testcase}}.yml
