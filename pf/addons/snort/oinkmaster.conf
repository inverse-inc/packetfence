# oinkmaster.conf
#
# See Oinkmaster section of the PacketFence Adminstration Guide for instructions
#

url = http://rules.emergingthreats.net/open/snort-2.8.6/emerging.rules.tar.gz
path = /bin:/usr/bin:/usr/local/bin
update_files = \.rules$|\.config$|\.conf$|\.txt$|\.map$

skipfile local.rules
skipfile deleted.rules
skipfile snort.conf

localsid 1100000
localsid 1100001
localsid 1100002
localsid 1100003
localsid 1100004
localsid 1100005
localsid 1100006
localsid 1100007
localsid 1100008
localsid 1100009

# rewrite rules with packetfence's snort variables
modifysid * "udp any" | "udp \$HOME_NET"
modifysid * "tcp any" | "tcp \$HOME_NET"
modifysid * "icmp any" | "icmp \$HOME_NET"
modifysid * "(tcp|udp) any" | "\1 \$HOME_NET"

# add your own rule exceptions here
# example
#disablesid 2001011
